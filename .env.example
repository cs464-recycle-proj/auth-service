# =========================
# JWT Configuration
# =========================
# Base64-encoded HMAC-SHA256 secret (must be 256 bits / 32 bytes)
# Generate using:
#   openssl rand -base64 32
JWT_SECRET=your-base64-encoded-256-bit-secret-key-here

# Token expiration time in milliseconds (default: 24h = 86400000)
JWT_EXPIRATION_TIME=86400000

# JWT issuer (shown in the "iss" field)
JWT_ISSUER=auth-service


# =========================
# Database Configuration
# =========================
# PostgreSQL connection string
# For Docker: host.docker.internal allows containers to access the host DB.
DATABASE_URL=jdbc:postgresql://host.docker.internal:5432/your-database-name

# DB credentials
DATABASE_USERNAME=your-db-username
DATABASE_PASSWORD=your-db-password


# =========================
# Resend Email API (for OTP & notifications)
# =========================
# Your Resend API key
RESEND_API_KEY=your-resend-api-key

# Default sender email registered in Resend
RESEND_FROM_EMAIL=your-email@your-domain.com


# =========================
# Optional Overrides
# =========================
# Custom server port (default: 8081)
# SERVER_PORT=8081

# Override OTP expiry (default: 5 minutes)
# OTP_EXPIRATION_MINUTES=5
